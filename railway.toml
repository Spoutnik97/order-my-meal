[build]
builder = "NIXPACKS"
buildCommand = "npm_config_build_from_source=true pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[mounts]]
mountPath = "/app/data"
